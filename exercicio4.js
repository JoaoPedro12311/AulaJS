var base = prompt("Digite a base do triângulo: ");

var altura = prompt("Digite a altura do triângulo: ");

var area = (Number(base) * Number(altura)) / 2;

document.write("A área do triângulo é: " + area.toFixed(2));